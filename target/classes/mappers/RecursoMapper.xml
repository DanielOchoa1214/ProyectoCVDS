<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="edu.eci.proyectoCVDS.persistence.mybatisimpl.mappers.RecursoMapper">

    <resultMap type='Recurso' id='RecursoResult'>
        <id property='id' column='id'/>
        <result property='name' column='name'/>
        <result property='location' column='location'/>
        <result property='type' column='type'/>
        <result property='capacity' column='capacity'/>
        <result property='bookingScheduleStart' column='booking_schedule_start'/>
        <result property='bookingScheduleEnd' column='booking_schedule_end'/>
        <result property='resourceState' column='resource_state'/>
    </resultMap>

    <!--    PROBAR CUANDO HAYA DB   -->
    <select id="getResource" resultMap="RecursoResult">
        SELECT
            r.name AS name,
            r.location AS location,
            r.type AS type,
            r.capacity AS capacity,
            r.id AS id,
            r.booking_schedule_start AS booking_schedule_start,
            r.booking_schedule_end AS booking_schedule_start,
            r.resource_state AS resource_state
        FROM
            RECURSO as r
        WHERE
            r.name = #{name} AND r.type = #{type} AND
            (#{capacity} = 0 OR  r.capacity = #{capacity}) AND
            (#{location} = '' OR  r.location = #{location})
    </select>

    <insert id="setNewRecurso">
        insert into RECURSO (name, location, type, capacity, id, booking_schedule_start, booking_schedule_end, resource_state)
        values (#{name}, #{location}, #{type}, #{capacity}, #{id}, #{bookingScheduleStart}, #{bookingScheduleEnd}, #{resourceState})
    </insert>

    <update id="updateResourceState">
        UPDATE RECURSO SET
            resource_state = #{estadoRecurso}
        WHERE id = #{id}
    </update>
</mapper>